# ADR-<NNNN> — <Título curto, imperativo>

## Status
Proposto | Aceito | Substituído por ADR-<MMMM> | Depreciado em <YYYY-MM-DD>

## Data
YYYY-MM-DD

## Autor humano
@<handle> (ou nome, sem co-autoria de IA)

## Contexto
Por que esta decisão importa agora. Restrição, demanda, evidência empírica que motivou. ≤ 5 linhas.

## Alternativas consideradas

| # | Opção | Prós | Contras |
|:---:|---|---|---|
| 1 | <descrição curta> | <prós> | <contras> |
| 2 | <descrição curta> | <prós> | <contras> |
| 3 | <descrição curta opcional> | <prós> | <contras> |

(Mínimo 2 alternativas — invariante de co-autoria, herança RN-D-001 do mdcu.)

## Decisão
Escolha + justificativa em 2–4 linhas. Marque `[F]` fato empírico, `[I]` inferência, `[P]` proposta — convenção do framework (`_self_check.md`).

## Consequências
- **Positivas**: ...
- **Negativas / custo aceito**: ...
- **Neutras / efeitos secundários**: ...

## Blast radius
Células de `traceability/spec-impact.md` afetadas por esta decisão:
- 🟥 `<entidade>` × `<componente>` — descrição da quebra
- 🟨 `<entidade>` × `<componente>` — descrição da atenção
- 🟩 `<entidade>` × `<componente>` — descrição da indireção

(Lista deve ser idêntica ao que será refletido em `traceability/spec-impact.md` após este ADR. Vazio só se a decisão for puramente de processo, não estrutural.)

## Referências
- Issue: #<N>
- PR (caso já exista): #<M>
- ADR relacionado: ADR-<NNNN>
- Postmortem (se aplicável): `docs/postmortems/<arquivo>.md`
- Outros: links externos, papers, etc.

## Changelog desta ADR
- YYYY-MM-DD — criada (status: Proposto)
- YYYY-MM-DD — aceita (status: Aceito)
- YYYY-MM-DD — substituída por ADR-<MMMM>
